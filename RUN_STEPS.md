# دستورات اجرای مراحل 1-4

## ⚠️ مرحله 1: Docker Desktop (نیاز به شما)

**شما باید Docker Desktop را باز کنید:**

1. از Start Menu: `Docker Desktop` را جستجو و اجرا کنید
2. یا از system tray (کنار ساعت) روی آیکون Docker کلیک کنید
3. منتظر بمانید تا کاملاً راه‌اندازی شود (آیکون سبز شود)
   - این ممکن است 1-2 دقیقه طول بکشد

## ✅ پس از راه‌اندازی Docker Desktop

این دستورات را در PowerShell اجرا کنید:

### مرحله 2: راه‌اندازی PostgreSQL
```powershell
docker-compose up -d postgres
```

**⏱️ صبر کنید:** 30 ثانیه تا PostgreSQL کاملاً آماده شود

### مرحله 3: Initialize دیتابیس
```powershell
python scripts/init_database.py
```

### مرحله 4: تولید داده
```powershell
python scripts/generate_and_display_mri_data.py
```

## 📋 دستورات کامل (کپی-پیست)

```powershell
# مرحله 2: راه‌اندازی PostgreSQL
docker-compose up -d postgres

# صبر کنید (30 ثانیه)
Start-Sleep -Seconds 30

# مرحله 3: Initialize دیتابیس
python scripts/init_database.py

# مرحله 4: تولید داده
python scripts/generate_and_display_mri_data.py
```

## ✅ بررسی نتیجه

پس از اجرای دستورات، بررسی کنید:

```powershell
python -c "import requests; r = requests.get('http://localhost:8001/api/v1/patients/'); print(f'تعداد بیماران: {len(r.json())}')"
```

یا در Frontend:
- به صفحه **Dashboard** بروید و Hard Refresh کنید (Ctrl+F5)
- مقادیر باید به‌روزرسانی شوند

## 🎯 نتیجه نهایی

پس از انجام این مراحل:
- ✅ 50 بیمار در دیتابیس ذخیره می‌شود
- ✅ Dashboard آمار بیماران را نمایش می‌دهد
- ✅ صفحه Patients لیست بیماران را نمایش می‌دهد

