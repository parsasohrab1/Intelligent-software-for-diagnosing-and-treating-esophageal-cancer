# رفع مشکل: داده‌ها در Dashboard بارگذاری نمی‌شوند

## 🔍 تشخیص مشکل

از تصویر Dashboard می‌بینیم که:
- ✅ Dashboard کار می‌کند و نمایش داده می‌شود
- ✅ API endpointها پاسخ می‌دهند (Status: 200)
- ❌ همه مقادیر 0 هستند (Total Patients: 0, Datasets: 0, ML Models: 0)
- ❌ PostgreSQL در حال اجرا نیست
- ❌ داده‌ای در دیتابیس وجود ندارد

## 🚀 راه‌حل

### مرحله 1: Docker Desktop را باز کنید

1. **Docker Desktop را اجرا کنید:**
   - از Start Menu: `Docker Desktop` را جستجو کنید
   - یا از system tray (کنار ساعت) روی آیکون Docker کلیک کنید

2. **منتظر بمانید:**
   - Docker Desktop باید کاملاً راه‌اندازی شود
   - آیکون Docker در system tray باید سبز شود
   - این ممکن است 1-2 دقیقه طول بکشد

### مرحله 2: PostgreSQL را راه‌اندازی کنید

```powershell
docker-compose up -d postgres
```

**⏱️ صبر کنید:** 30 ثانیه تا PostgreSQL کاملاً آماده شود

### مرحله 3: دیتابیس را Initialize کنید

```powershell
python scripts/init_database.py
```

این دستور جداول دیتابیس را ایجاد می‌کند.

### مرحله 4: داده تولید کنید

```powershell
python scripts/generate_and_display_mri_data.py
```

این اسکریپت:
- ✅ 50 بیمار تولید می‌کند
- ✅ 40% از بیماران سرطان دارند (20 بیمار)
- ✅ 60% از بیماران عادی هستند (30 بیمار)
- ✅ شامل داده‌های MRI می‌شود
- ✅ همه داده‌ها را در دیتابیس ذخیره می‌کند

## ✅ نتیجه

پس از انجام این مراحل:
- **Dashboard** آمار بیماران را نمایش می‌دهد:
  - Total Patients: 50
  - Cancer Patients: 20
  - Normal Patients: 30
- **صفحه Patients** لیست 50 بیمار را نمایش می‌دهد
- **Datasets** و **ML Models** هم به‌روزرسانی می‌شوند

## 🔍 بررسی وضعیت

برای بررسی اینکه داده تولید شده:

```powershell
# بررسی تعداد بیماران
python -c "import requests; r = requests.get('http://localhost:8001/api/v1/patients/'); print(f'تعداد بیماران: {len(r.json())}')"
```

یا مستقیماً در Frontend:
- به صفحه **Dashboard** بروید
- مقادیر باید به‌روزرسانی شوند
- به صفحه **Patients** بروید
- لیست بیماران باید نمایش داده شود

## 📝 دستورات کامل (کپی-پیست)

```powershell
# 1. راه‌اندازی PostgreSQL
docker-compose up -d postgres

# 2. صبر کنید (30 ثانیه)
Start-Sleep -Seconds 30

# 3. Initialize دیتابیس
python scripts/init_database.py

# 4. تولید داده
python scripts/generate_and_display_mri_data.py
```

## ⚠️ نکات مهم

1. **Docker Desktop باید همیشه در حال اجرا باشد** تا PostgreSQL کار کند
2. **Backend باید در حال اجرا باشد** (پورت 8001)
3. **Frontend باید در حال اجرا باشد** (پورت 3000)
4. پس از تولید داده، **Dashboard را refresh کنید** (Ctrl+F5)

## 🐛 مشکلات رایج

### Dashboard هنوز 0 نمایش می‌دهد
**راه‌حل:**
1. بررسی کنید PostgreSQL در حال اجرا است: `docker-compose ps postgres`
2. بررسی کنید داده تولید شده: `python -c "import requests; print(len(requests.get('http://localhost:8001/api/v1/patients/').json()))"`
3. Dashboard را Hard Refresh کنید (Ctrl+F5)
4. Backend را restart کنید

### خطا: "Docker Desktop is not running"
**راه‌حل:** Docker Desktop را باز کنید و منتظر بمانید تا راه‌اندازی شود.

### خطا: "Connection refused" در PostgreSQL
**راه‌حل:** 
1. بررسی کنید Docker Desktop در حال اجرا است
2. `docker-compose up -d postgres` را دوباره اجرا کنید
3. 30 ثانیه صبر کنید

